# MiroThinker 项目理解文档

## 🎯 项目概述

**MiroThinker** 是由 MiroMind 团队开发的开源**搜索智能体（Research Agent）模型**，旨在推进工具增强推理（Tool-Augmented Reasoning）和信息检索能力，使模型能够处理复杂的真实世界研究任务。

---

## 🧠 核心原理

### 1. 交互式扩展（Interactive Scaling）

传统 LLM 的扩展方式主要集中在两个维度：
1. **模型规模扩展**：增加模型参数量
2. **上下文长度扩展**：增加上下文窗口大小

MiroThinker 引入了**第三维度**——**交互式扩展**：
- 训练模型处理**更深层次、更频繁的智能体-环境交互**
- 利用**环境反馈和外部信息获取**来纠正错误、优化推理轨迹
- 支持 **256K 上下文窗口**和单任务最多 **400-600 次工具调用**

```
┌──────────────────────────────────────────────────────────────────┐
│                      交互式扩展示意图                              │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│    传统扩展                    MiroThinker 扩展                  │
│    ─────────                   ─────────────────                 │
│    • 模型规模 ↑                 • 模型规模 ↑                      │
│    • 上下文长度 ↑               • 上下文长度 ↑                    │
│                                • ⭐ 交互深度 ↑ (新维度)           │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 2. ReAct 范式

MiroThinker 采用 **ReAct (Reasoning + Acting)** 推理范式：

```
循环执行：
1. 思考（Thought）: LLM 分析当前状态，决定下一步行动
2. 行动（Action）: 调用外部工具执行操作
3. 观察（Observation）: 获取工具返回结果
4. 重复直到得出最终答案
```

### 3. 上下文保留策略（Context Retention）

为避免长对话中上下文溢出，项目实现了**基于新近度的上下文保留策略（Recency-Based Context Retention）**：

- 仅保留最近 **K 个**工具调用的响应结果（如 `keep_tool_result: 5`）
- 保留完整的**思考和行动序列**
- 优点：
  - ✅ 保留推理轨迹
  - ✅ 聚焦最相关的近期观察
  - ✅ 释放上下文空间用于更深层推理
  - ✅ 不降低性能的前提下支持更多交互

---

## 🏗️ 项目架构

```
MiroThinker/
├── apps/                            # 应用程序目录
│   ├── miroflow-agent/              # ⭐ 核心智能体框架
│   │   ├── main.py                  # 程序入口
│   │   ├── conf/                    # Hydra 配置文件
│   │   │   ├── agent/               # 智能体配置 (如 mirothinker_v1.5_keep5_max200.yaml)
│   │   │   ├── llm/                 # LLM 模型配置
│   │   │   └── benchmark/           # 基准测试配置
│   │   ├── src/
│   │   │   ├── core/                # 核心模块
│   │   │   │   ├── pipeline.py      # 任务执行管线
│   │   │   │   ├── orchestrator.py  # 智能体编排器 (核心)
│   │   │   │   ├── tool_executor.py # 工具执行器
│   │   │   │   └── stream_handler.py# 流式处理
│   │   │   ├── llm/                 # LLM 客户端
│   │   │   ├── io/                  # 输入输出格式化
│   │   │   └── logging/             # 日志记录
│   │   ├── benchmarks/              # 基准测试代码
│   │   └── scripts/                 # 评估脚本
│   ├── collect-trace/               # 轨迹收集（用于 SFT/DPO 训练）
│   ├── gradio-demo/                 # Gradio 演示界面
│   └── visualize-trace/             # 轨迹可视化
│
├── libs/                            # 库目录
│   └── miroflow-tools/              # ⭐ 工具管理库
│       └── src/miroflow_tools/
│           ├── manager.py           # ToolManager 核心类
│           ├── mcp_servers/         # MCP 服务器集合
│           │   ├── python_mcp_server.py     # Python/代码执行
│           │   ├── vision_mcp_server.py     # 视觉问答
│           │   ├── audio_mcp_server.py      # 音频转录
│           │   ├── reading_mcp_server.py    # 文档阅读
│           │   ├── reasoning_mcp_server.py  # 推理引擎
│           │   └── serper_mcp_server.py     # Google 搜索
│           └── dev_mcp_servers/     # 开发版 MCP 服务器
│
└── assets/                          # 静态资源
```

---

## ⚙️ 运行逻辑

### 整体流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           MiroThinker 运行流程                           │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                     ┌──────────────┴──────────────┐
                     ▼                             ▼
              ┌─────────────┐              ┌─────────────────┐
              │  main.py    │              │  Hydra Config   │
              │  (入口)     │◄─────────────│  (配置加载)     │
              └──────┬──────┘              └─────────────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │  create_pipeline_   │   初始化组件：
          │  components()       │   • ToolManager (主智能体)
          └─────────┬───────────┘   • ToolManager (子智能体，可选)
                    │               • OutputFormatter
                    ▼
          ┌─────────────────────┐
          │  execute_task_      │   创建：
          │  pipeline()         │   • LLM Client
          └─────────┬───────────┘   • Orchestrator
                    │               • TaskLog
                    ▼
          ┌─────────────────────┐   ┌─────────────────────────────┐
          │                     │   │  run_main_agent() 循环：     │
          │    Orchestrator     │──▶│  1. 构建提示 + 消息历史       │
          │  (核心编排器)        │   │  2. 调用 LLM                 │
          │                     │   │  3. 解析工具调用              │
          └─────────────────────┘   │  4. 执行工具                  │
                    │               │  5. 处理结果                  │
                    │               │  6. 更新消息历史              │
                    ▼               │  7. 重复直到完成              │
          ┌─────────────────────┐   └─────────────────────────────┘
          │  Final Answer       │
          │  (最终答案)         │
          └─────────────────────┘
```

### 详细步骤解析

#### 1️⃣ 初始化阶段

```python
# main.py
@hydra.main(config_path="conf", config_name="config")
def main(cfg: DictConfig):
    asyncio.run(amain(cfg))
```

- 使用 **Hydra** 配置框架加载配置
- 配置决定使用的模型、智能体类型、工具集

#### 2️⃣ 组件创建

```python
# pipeline.py
def create_pipeline_components(cfg):
    # 为主智能体创建 ToolManager
    main_agent_tool_manager = ToolManager(main_agent_mcp_server_configs, ...)
    
    # 为子智能体创建 ToolManager（如果是多智能体模式）
    sub_agent_tool_managers = {...}
    
    # 输出格式化器
    output_formatter = OutputFormatter()
    
    return main_agent_tool_manager, sub_agent_tool_managers, output_formatter
```

#### 3️⃣ 任务执行管线

```python
# pipeline.py
async def execute_task_pipeline(...):
    # 创建任务日志
    task_log = TaskLog(...)
    
    # 初始化 LLM 客户端
    llm_client = ClientFactory(cfg=cfg, ...)
    
    # 初始化编排器
    orchestrator = Orchestrator(
        main_agent_tool_manager=main_agent_tool_manager,
        llm_client=llm_client,
        ...
    )
    
    # 运行主智能体
    final_summary, final_boxed_answer, _ = await orchestrator.run_main_agent(
        task_description=task_description,
        ...
    )
```

#### 4️⃣ Orchestrator 核心循环

`orchestrator.py` 中的 `run_main_agent()` 方法实现核心逻辑：

```python
async def run_main_agent(self, task_description, ...):
    """
    执行主智能体的端到端任务。
    核心循环：
    1. 构建系统提示和消息历史
    2. 调用 LLM 获取响应
    3. 解析响应中的工具调用
    4. 执行工具调用
    5. 将结果添加到消息历史
    6. 检查是否达到终止条件
    7. 重复直到完成或达到最大轮次
    """
    message_history = []
    turn_count = 0
    max_turns = cfg.agent.main_agent.max_turns  # 如 200 或 400
    
    while turn_count < max_turns:
        # 调用 LLM
        response = await llm_client.chat(system_prompt, message_history)
        
        # 解析工具调用
        tool_calls = parse_tool_calls(response)
        
        if not tool_calls:
            # 没有工具调用，检查是否完成
            if contains_final_answer(response):
                break
        else:
            # 执行工具调用
            for tool_call in tool_calls:
                result = await tool_executor.execute(tool_call)
                # 添加到消息历史
                message_history.append(result)
        
        turn_count += 1
    
    return final_summary, final_boxed_answer
```

#### 5️⃣ 工具执行

通过 **MCP (Model Context Protocol)** 协议与外部工具通信：

```python
# ToolManager 执行工具调用
result = await tool_manager.execute_tool_call(
    server_name="tool-python",
    tool_name="google_search",
    arguments={"q": "MiroThinker paper"}
)
```

---

## 🔧 核心组件详解

### 1. ToolManager（工具管理器）

**位置**: `libs/miroflow-tools/src/miroflow_tools/manager.py`

**功能**:
- 管理多个 MCP 服务器的连接
- 获取所有可用工具的定义
- 执行工具调用并返回结果
- 工具黑名单过滤

### 2. Orchestrator（编排器）

**位置**: `apps/miroflow-agent/src/core/orchestrator.py`

**职责**:
- 协调 LLM 与工具之间的交互
- 管理消息历史
- 处理重复查询检测和回滚
- 支持主智能体和子智能体
- 实现上下文保留策略

### 3. MCP 服务器

MiroThinker v1.5 使用的核心工具：

| 服务器 | 功能 | 主要工具 |
|--------|------|----------|
| `tool-python` | 代码执行环境（E2B 沙箱） | `create_sandbox`, `run_python_code`, `run_command` |
| `search_and_scrape_webpage` | Google 搜索 | `google_search` |
| `jina_scrape_llm_summary` | 网页抓取 + LLM 摘要 | `scrape_and_extract_info` |

---

## 📊 模型版本对比

| 版本 | 基础模型 | 上下文长度 | 最大工具调用 | 特点 |
|------|----------|------------|--------------|------|
| v1.5-30B | Qwen3-30B-A3B-Thinking | 256K | 400 | 当前最佳，世界领先性能 |
| v1.5-235B | Qwen3-235B-A22B-Thinking | 256K | 400 | 最大规模，性能最强 |
| v1.0-8B | Qwen3-8B | 256K | 600 | 轻量级选择 |
| v1.0-30B | Qwen3-30B-A3B-Thinking | 256K | 600 | 平衡选择 |
| v1.0-72B | Qwen2.5-72B-Instruct | 256K | 600 | v1.0 系列最强 |

---

## 🚀 快速开始

### 环境配置

```bash
# 克隆项目
git clone https://github.com/MiroMindAI/MiroThinker
cd MiroThinker

# 安装依赖
cd apps/miroflow-agent
uv sync

# 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API 密钥
```

### 必需的 API 密钥

| 密钥 | 用途 | 获取地址 |
|------|------|----------|
| `SERPER_API_KEY` | Google 搜索 | [serper.dev](https://serper.dev/) |
| `JINA_API_KEY` | 网页抓取 | [jina.ai](https://jina.ai/) |
| `E2B_API_KEY` | 代码执行沙箱 | [e2b.dev](https://e2b.dev/) |
| `SUMMARY_LLM_*` | 内容摘要 | 你的 LLM 服务 |

### 运行示例

```bash
# 使用 Claude
uv run python main.py llm=claude-3-7 agent=single_agent_keep5

# 使用自托管的 MiroThinker 模型
uv run python main.py llm=qwen-3 agent=mirothinker_v1.5_keep5_max200 \
    llm.base_url=http://localhost:61002/v1
```

---

## 📚 相关项目

| 项目 | 说明 |
|------|------|
| [MiroFlow](https://github.com/MiroMindAI/MiroFlow) | 可复现的 SOTA 研究智能体框架 |
| [MiroVerse](https://huggingface.co/datasets/miromind-ai/MiroVerse-v0.1) | 147K 样本的高质量训练数据集 |
| [MiroTrain](https://github.com/MiroMindAI/MiroTrain) | 研究智能体训练基础设施 |
| [MiroRL](https://github.com/MiroMindAI/MiroRL) | 强化学习训练框架 |

---

## 📄 引用

```bibtex
@article{miromind2025mirothinker,
  title={MiroThinker: Pushing the Performance Boundaries of Open-Source Research Agents 
         via Model, Context, and Interactive Scaling},
  author={MiroMind Team and ...},
  journal={arXiv preprint arXiv:2511.11793},
  year={2025}
}
```
