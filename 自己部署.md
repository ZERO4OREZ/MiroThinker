# MiroThinker 本地部署指南

> 本文档详细介绍如何在本地部署和运行 MiroThinker 项目。

---

## 📋 目录

- [前置要求](#-前置要求)
- [部署方式选择](#-部署方式选择)
- [方式一：使用云端 LLM API（推荐新手）](#方式一使用云端-llm-api推荐新手)
- [方式二：本地部署 MiroThinker 模型](#方式二本地部署-mirothinker-模型)
- [方式三：Gradio 演示界面](#方式三gradio-演示界面)
- [常见问题排查](#-常见问题排查)

---

## 🔧 前置要求

### 软件环境

| 软件 | 要求 | 安装命令 |
|------|------|----------|
| Python | 3.10+ | - |
| uv | 最新版 | `curl -LsSf https://astral.sh/uv/install.sh \| sh` |
| Git | 最新版 | - |

### API 密钥（必需）

无论使用哪种部署方式，都需要以下 API 密钥：

| 服务 | 用途 | 获取地址 | 免费额度 |
|------|------|----------|----------|
| **Serper** | Google 搜索 | [serper.dev](https://serper.dev/) | 新用户 2,500 次免费 |
| **E2B** | 代码执行沙箱 | [e2b.dev](https://e2b.dev/) | 有免费额度 |
| **Jina** | 网页抓取 | [jina.ai](https://jina.ai/) | 有免费额度 |

---

## 🎯 部署方式选择

| 方式 | 适用场景 | 硬件要求 | 难度 |
|------|----------|----------|------|
| [方式一：云端 LLM API](#方式一使用云端-llm-api推荐新手) | 快速体验，无需 GPU | 普通电脑 | ⭐ 简单 |
| [方式二：本地部署模型](#方式二本地部署-mirothinker-模型) | 完全本地化，追求性能 | 16GB+ VRAM GPU | ⭐⭐⭐ 进阶 |
| [方式三：Gradio 演示](#方式三gradio-演示界面) | 可视化 Web 界面 | 16GB+ VRAM GPU | ⭐⭐ 中等 |


### 💡 理解项目组成

```
MiroThinker 项目 = 框架 + 工具库 + 模型

├── MiroFlow-Agent (框架)     ← 方式一 ✅ 使用
├── MiroFlow-Tools (工具库)   ← 方式一 ✅ 使用
└── MiroThinker 模型          ← 方式一 ❌ 不使用（用 Claude/GPT 替代）
```

> **⚠️ 注意**：方式一并未使用 MiroThinker 模型本身，而是用 Claude/GPT 作为 LLM，仅利用了项目的框架和工具库。如需使用 MiroThinker 模型，请选择**方式二**。

**方式一的工作原理：**
- 🧠 用 **Claude/GPT** 作为 LLM（大脑）
- 🔧 用 **MiroFlow 框架** 做智能体编排
- 🌐 调用**外部工具**（搜索、代码执行等）

---

## 方式一：使用云端 LLM API（推荐新手）

> 使用 Claude/GPT 等云端 API，无需本地 GPU，最快上手方式。

### 步骤 1：克隆项目

```bash
git clone https://github.com/MiroMindAI/MiroThinker
cd MiroThinker
```

### 步骤 2：安装依赖

```bash
cd apps/miroflow-agent
uv sync
```

### 步骤 3：配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env
```

编辑 `.env` 文件，填入以下内容：

```bash
# ===== 必需配置 =====

# Google 搜索 API
SERPER_API_KEY=你的_serper_key
SERPER_BASE_URL="https://google.serper.dev"

# 网页抓取 API
JINA_API_KEY=你的_jina_key
JINA_BASE_URL="https://r.jina.ai"

# 代码执行沙箱 API
E2B_API_KEY=你的_e2b_key

# ===== 根据使用的 LLM 选择配置 =====

# 如果使用 Claude（推荐）
ANTHROPIC_API_KEY=你的_anthropic_key
ANTHROPIC_BASE_URL=https://api.anthropic.com

# 如果使用 GPT
OPENAI_API_KEY=你的_openai_key
OPENAI_BASE_URL=https://api.openai.com/v1

# ===== 网页摘要 LLM（推荐配置）=====
SUMMARY_LLM_BASE_URL=https://api.openai.com/v1
SUMMARY_LLM_MODEL_NAME=gpt-4o-mini
SUMMARY_LLM_API_KEY=你的_openai_key
```

### 步骤 4：运行测试

```bash
# 使用 Claude 3.7
uv run python main.py llm=claude-3-7 agent=single_agent_keep5

# 或使用 GPT-5
uv run python main.py llm=gpt-5 agent=single_agent_keep5
```

### 步骤 5：自定义问题

编辑 `main.py` 第 32 行:

```python
task_description = "你想问的问题"
```

然后重新运行即可。

---

## 方式二：本地部署 MiroThinker 模型

> 完全本地化运行，需要 GPU，性能最佳。

### 硬件要求

| 配置 | GPU VRAM | 说明 |
|------|----------|------|
| 最低配置 | 16GB | 需使用 Q4 量化 (llama.cpp) |
| 推荐配置 | 48GB+ | 可用 FP8 量化，更好性能 |
| 最佳配置 | 80GB+ (如 A100) | 可运行完整精度 |

### 步骤 1：克隆项目并安装依赖

```bash
git clone https://github.com/MiroMindAI/MiroThinker
cd MiroThinker
cd apps/miroflow-agent
uv sync
```

### 步骤 2：下载模型

```python
# Python 方式下载
from huggingface_hub import snapshot_download
snapshot_download(
    repo_id="miromind-ai/MiroThinker-v1.5-30B",
    local_dir="model/MiroThinker-v1.5-30B"
)
```

或使用命令行：

```bash
# 安装 huggingface_hub
pip install huggingface_hub

# 下载模型
huggingface-cli download miromind-ai/MiroThinker-v1.5-30B --local-dir model/MiroThinker-v1.5-30B
```

### 步骤 3：启动模型服务

#### 选项 A：使用 SGLang（推荐，48GB+ VRAM）

```bash
# 安装 SGLang
pip install sglang

# 启动服务（FP8 量化，节省显存）
MODEL_PATH=model/MiroThinker-v1.5-30B
python3 -m sglang.launch_server \
    --model-path $MODEL_PATH \
    --mem-fraction-static 0.9 \
    --quantization fp8 \
    --tp 1 \
    --dp 1 \
    --host 0.0.0.0 \
    --port 61002 \
    --trust-remote-code
```

#### 选项 B：使用 llama.cpp（16GB VRAM）

```bash
# 下载量化版模型
wget https://huggingface.co/mradermacher/MiroThinker-v1.5-30B-GGUF/resolve/main/MiroThinker-v1.5-30B.Q4_K_M.gguf

# 启动服务
llama-server -m MiroThinker-v1.5-30B.Q4_K_M.gguf \
    --port 61002 \
    -ngl 99 \
    -v
```

#### 选项 C：使用 vLLM

```bash
pip install vllm

vllm serve miromind-ai/MiroThinker-v1.5-30B \
    --port 61002 \
    --trust-remote-code
```

### 步骤 4：配置环境变量

编辑 `.env` 文件:

```bash
# 必需的工具 API
SERPER_API_KEY=你的_serper_key
SERPER_BASE_URL="https://google.serper.dev"
JINA_API_KEY=你的_jina_key
JINA_BASE_URL="https://r.jina.ai"
E2B_API_KEY=你的_e2b_key

# 网页摘要 LLM（可以用本地模型）
SUMMARY_LLM_BASE_URL=http://localhost:61002/v1
SUMMARY_LLM_MODEL_NAME=MiroThinker
SUMMARY_LLM_API_KEY=none
```

### 步骤 5：运行智能体

```bash
cd apps/miroflow-agent

# 使用 MiroThinker v1.5（推荐）
uv run python main.py \
    llm=qwen-3 \
    agent=mirothinker_v1.5_keep5_max200 \
    llm.base_url=http://localhost:61002/v1
```

---

## 方式三：Gradio 演示界面

> 提供可视化 Web 界面，适合演示和交互使用。

### 步骤 1：完成方式二的模型部署

确保模型服务已启动，监听在 `http://localhost:61002/v1`

### 步骤 2：配置环境变量

编辑 `apps/miroflow-agent/.env`:

```bash
SERPER_API_KEY=你的_serper_key
E2B_API_KEY=你的_e2b_key
JINA_API_KEY=你的_jina_key
SUMMARY_LLM_BASE_URL=http://localhost:61002/v1
SUMMARY_LLM_MODEL_NAME=MiroThinker
SUMMARY_LLM_API_KEY=none
```

### 步骤 3：安装并启动 Gradio Demo

```bash
cd apps/gradio-demo
uv sync

# 设置 LLM 服务地址
export BASE_URL=http://localhost:61002/v1
export API_KEY=none  # 如果无需认证

# 启动
uv run main.py
```

### 步骤 4：访问 Web 界面

打开浏览器访问: `http://localhost:8080`

---

## 📊 运行基准测试（可选）

如果你想在标准基准测试上评估模型：

### 步骤 1：下载测试数据

```bash
cd MiroThinker  # 回到项目根目录
wget https://huggingface.co/datasets/miromind-ai/MiroFlow-Benchmarks/resolve/main/data_20251115_password_protected.zip
unzip data_20251115_password_protected.zip
# 密码: pf4*
rm data_20251115_password_protected.zip
```

### 步骤 2：运行评估

```bash
cd apps/miroflow-agent

# 运行 GAIA 基准测试
NUM_RUNS=8 \
LLM_MODEL="MiroThinker-v1.5-30B" \
BASE_URL="http://localhost:61002/v1" \
AGENT_SET="mirothinker_v1.5_keep5_max200" \
bash scripts/run_evaluate_multiple_runs_gaia-validation-text-103.sh
```

---

## ❓ 常见问题排查

### Q1: API 密钥错误

**症状**: `401 Unauthorized` 或 `Invalid API Key`

**解决**:
1. 检查 `.env` 文件中的 API 密钥是否正确
2. 确认密钥没有多余空格
3. 验证 API 密钥在官网仍然有效

### Q2: 模型服务连接失败

**症状**: `Connection refused` 或 `Failed to connect`

**解决**:
1. 确认模型服务已启动: `curl http://localhost:61002/v1/models`
2. 检查端口是否被占用
3. 确认 `llm.base_url` 配置正确（需要以 `/v1` 结尾）

### Q3: GPU 显存不足

**症状**: `CUDA out of memory`

**解决**:
1. 使用更小的量化版本 (Q4_K_M)
2. 减少并发数: `MAX_CONCURRENT=5`
3. 使用上下文管理配置: `agent=mirothinker_v1.5_keep5_max200`

### Q4: 工具执行错误

**症状**: `E2B/Serper/Jina execution failed`

**解决**:
1. **E2B**: 检查账户是否有余额
2. **Serper**: 检查搜索配额
3. **Jina**: 检查 API 配额

### Q5: uv 命令找不到

**解决**:
```bash
# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# 重新加载环境
source ~/.bashrc  # 或 ~/.zshrc
```

---

## 📁 重要文件路径

| 文件 | 路径 | 说明 |
|------|------|------|
| 主程序入口 | `apps/miroflow-agent/main.py` | 修改问题在此 |
| 环境变量 | `apps/miroflow-agent/.env` | API 密钥配置 |
| LLM 配置 | `apps/miroflow-agent/conf/llm/*.yaml` | 模型参数 |
| 智能体配置 | `apps/miroflow-agent/conf/agent/*.yaml` | 智能体行为 |
| 日志输出 | `logs/` | 运行日志 |

---

## 🎉 验证部署成功

运行以下命令测试:

```bash
cd apps/miroflow-agent
uv run python main.py llm=claude-3-7 agent=single_agent_keep5
```

如果看到类似输出，则部署成功:

```
--- Starting Task Execution: task_example ---
[调用 google_search 工具...]
[调用 scrape_and_extract_info 工具...]
...
Final Answer: xxx
```

---

## 📞 获取帮助

- 📖 项目文档: [README.md](./README.md)
- 💬 Discord 社区: [加入](https://discord.com/invite/GPqEnkzQZd)
- 🐛 问题反馈: [GitHub Issues](https://github.com/MiroMindAI/MiroThinker/issues)
